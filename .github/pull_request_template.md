## What & why

## How it was tested

- [ ] `npm test` passes
- [ ] New behavior has tests (fixtures are real git repos via `makeRepo()`)
- [ ] No new runtime dependencies (or discussed in an issue)
- [ ] Plugin regenerated if `src/integrations/claude.ts` changed (`npm run build:plugin`)
